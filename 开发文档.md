# 《抢词夺力》开发文档

> 📋 面向 AI Agent 的开发参考文档  
> 📅 最后更新：2026-07-10  

---

## 一、项目结构

```
Grab-Word/
├── index.html          # 主文件（全部前端逻辑内联，约3370行）
├── dungeon.js          # 地牢模式 DLC 外置模块（约680行）
├── words.json          # 词库数据（高中/初中/小学）
├── 字母手牌.txt        # 手牌字母池配置
├── test.bat            # Windows 快速测试启动
├── .gitattributes      # Git 配置
├── .gitignore
├── package.json
└── openspec/           # OpenSpec 工作流目录（变更管理）
    ├── config.yaml
    ├── changes/
    │   └── archive/
    └── specs/
```

---

## 二、运行方式

单文件 H5 应用。浏览器直接打开 `index.html` 即可运行。`dungeon.js` 通过 `<script>` 标签在 `index.html` 末尾引入，所有逻辑均为纯前端，无服务端依赖。

`test.bat` 使用 `npx http-server` 启动本地静态服务器（解决跨域/file协议问题）。

---

## 三、架构概览

```
┌──────────────────────────────────────────────┐
│                    index.html                  │
│  ┌────────────────────────────────────────┐  │
│  │  CSS (line 8-940)                        │  │
│  │  含 PVP 双人、SOLO 单人、主界面、弹窗     │  │
│  └────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────┐  │
│  │  HTML 结构 (line 943-1127)              │  │
│  │  #mainMenu / #left/#right PVP split    │  │
│  │  #soloPanel  (单人模式面板)             │  │
│  │  #pvpCenter  (双人对战中心区)           │  │
│  │  #dungeonShopArea (地牢商城容器)        │  │
│  │  overlays: 结算/设置/排行榜/规则        │  │
│  └────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────┐  │
│  │  JS (line 1190-3365)                   │  │
│  │  全局状态 / PVP模式 / SOLO模式          │  │
│  │  设置&存档 / 词库管理 / 控制台命令      │  │
│  └────────────────────────────────────────┘  │
├──────────────────────────────────────────────┤
│  <script src="dungeon.js"> (line 3367)       │
│  ┌────────────────────────────────────────┐  │
│  │  地牢模式 IIFE                          │  │
│  │  属性/金币/压力/诅咒/道具/商城          │  │
│  │  暴露 window.Dungeon 接口给主文件调用   │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

---

## 四、全局状态对象

### 4.1 `G` — PVP 双人模式状态

| 字段 | 类型 | 说明 |
|------|------|------|
| `running` | bool | 游戏进行中 |
| `handSize` | int | 手牌数量 |
| `hands[2] | 双方手牌 |
| `slots` | array | 当前单词字母槽 |
| `currentWord` | {} | `{en, cn, bank}` |
| `score` | array[2] | 双方得分 |
| `turn` | int | 0=左玩家, 1=右玩家 |
| `timeLeft` | int | 全局倒计时（秒） |
| `cooldown` | bool | 玩家操作冷却 |
| `combo` | array[2] | 双方连击数 |
| `passed` | array[2] | 双方PASS状态 |
| `wordbook` | array[2] | 双方单词簿 |

### 4.2 `S` — SOLO 单人模式状态

| 字段 | 类型 | 说明 |
|------|------|------|
| `running` | bool | 游戏进行中 |
| `stageIdx` | int | 当前段位索引（0-9+） |
| `maxStageReached` | int | 本次最高段位 |
| `endlessLevel` | int | 无尽段位内小层级 |
| `totalScore` | int | 总分 |
| `stageScore` | int | 当前段位得分（用于升段判定） |
| `correctCount` | int | 升段条件计数 |
| `streakCount` | int | 隐藏升段计数 |
| `totalCorrect` | int | 总答对数 |
| `perfectCount` | int | 完美答题数（S评分） |
| `combo` | int | 当前连击 |
| `timeLeft` | float | 全局倒计时 |
| `lives` | int | 生命数（默认2） |
| `globalFlowRate` | float | 全局流速倍率 |
| `wordFlowRate` | float | 单词计时流速倍率 |
| `wordTimerMs` | float | 当前层剩余ms |
| `wordTimerLayer` | int | 当前层数 |
| `slots` | array | 字母槽 |
| `blankCount` | int | 空位数量 |
| `currentWord` | {} | 当前单词 |
| `isShiny` | bool | 当前词是否炫彩 |
| `wrongCount` | int | 本题错牌次数 |
| `burstCounter` | int | 连击爆发计数 |
| `burstQualities` | array | 连击爆发评分序列 |
| `promoReady` | bool | 是否升段题 |
| `transitioning` | bool | 题目切换过渡中 |
| `tierLock` | null/str | 段位锁定（'easy'/'medium'/'hard'） |

---

## 五、主文件模块划分

### 5.1 词库加载 & 基础工具

| 函数 | 说明 |
|------|------|
| `loadWordsFromDOM()` | 从 `words.json` 加载词库到 `WORDS_EASY/MED/HARD` |
| `shuffle(arr)` | Fisher-Yates 洗牌 |
| `randInt(min,max)` | 随机整数 |
| `playSound(freq,dur,vol)` | Web Audio 音效播放（方波） |

### 5.2 主界面 & 模式选择

| 函数 | 说明 |
|------|------|
| `selectMode(mode)` | 'solo'/'pvp' 模式切换，显示/隐藏对应面板 |
| `selectSubmode(sub)` | 'study'/'dungeon' 子模式切换 |
| `selectSoloTier(tier)` | 单人段位锁定选择 |
| `toggleSoloDiffPopup()` | 段位选择弹窗 |
| `renderScoreCard()` | 主界面分数牌 |
| `openLeaderboard()` | 排行榜弹窗 |

### 5.3 PVP 双人模式

| 函数 | 说明 |
|------|------|
| `startGame()` | 倒计时启动 |
| `beginGame()` | 初始化双人对战 |
| `pickWord()` | 从词库抽词 |
| `setupWord(winnerHand)` | 生成题目字母槽 |
| `dealHand(neededLetters,size)` | 发手牌 |
| `playCard(player,idx)` | 玩家出牌 |
| `checkWordComplete(lastPlayer)` | 检查单词是否完成 |
| `passAction(player)` | PASS 操作 |
| `endGame(byGap)` | 结束判定 |
| `pauseGame()/resumeGame()` | 暂停/恢复 |

### 5.4 SOLO 单人模式（核心）

| 函数 | 说明 |
|------|------|
| `beginSoloGame()` | 初始化单人局（含地牢初始化判断） |
| `soloStage()` | 获取当前段位配置 |
| `soloRollShiny()` | PRD 炫彩词判定（含地牢技巧加成） |
| `soloSetupWord()` | 生成题目（字母槽+提示） |
| `soloStartWordTimer()` | 启动单题计时（含地牢流速修正） |
| `soloPlayCard(idx)` | 出牌处理（含地牢压力累积） |
| `soloCheckComplete()` | 单词完成判定+计分 |
| `soloPromote()` | 升段处理（含地牢金币/压力/Boss/商城） |
| `soloDemote()` | 降段处理 |
| `soloPassAction()` | PASS（含地牢压力+3） |
| `soloEndGame()` | 结算（含地牢清理） |
| `soloAddWordBook()` | 记录单词簿（地牢模式跳过） |

### 5.5 段位系统

| 函数 | 说明 |
|------|------|
| `soloStage()` | 根据 `stageIdx` 返回段位配置（积分阈值/空格数/炫彩率/词库等） |
| `soloStageTier(idx)` | 返回大段位编号 0=小学/1=初中/2=高中/3=无尽 |
| `soloStageStars()` | 获取当前星级 |
| `soloUpdateStageUI()` | 更新段位 UI（名称+星星+积分条） |
| `soloUpdateLives()` | 生命值 UI |

### 5.6 存档 & 设置

| 函数 | 说明 |
|------|------|
| `loadSave()/writeSave()` | localStorage 读写 |
| `syncVersion()` | 版本兼容转换 |
| `getScoreRecords()/addScoreRecord()` | 分数排行榜（上限10条） |
| `canRecordRank()` | 记录能否上榜 |
| `exportSave()/importSave()` | 存档导入导出 |
| `getEffectiveWords()/getEffectiveHand()` | 获取生效的词库/手牌 |
| `exportLib()/importLib()` | 词库导入导出 |
| `getLocalHighestStage()/saveLocalHighestStage()` | 本地最高段位存储 |

### 5.7 控制台命令（调试用）

| 命令 | 功能 |
|------|------|
| `tower_up()` | 当前段位+1 |
| `tower_max()` | 直达无尽段位 |
| `set_point(n)` | 设置分数 |
| `game_over()` | 强制结束游戏 |

---

## 六、段位配置 (`SOLO_STAGES`)

数组索引 0-8 → 小学1星~高中3星。每项结构：

```js
{
  label: '高中 3星',
  bank: 'hard',          // 词库: 'easy'/'medium'/'hard'
  tier: 2,               // 大段位编号
  stars: 3,              // 星级
  threshold: 30,         // 升段积分阈值
  blanks: [5,7],         // 空格数范围
  timeDrop: 1.5,         // 答错扣时系数(已废弃)
  shinyC: 0.09,          // 炫彩概率基础值
  flowRate: 1.0,         // 流速(已废弃)
}
```

段位索引映射：
- 0~2: 小学1~3星
- 3~5: 初中1~3星  
- 6~8: 高中1~3星
- 9+: 无尽级（`endlessLevel` 小层级递增）

升段阈值递增规则：每3段阶梯式+5，无尽后每小段递增。

---

## 七、地牢模式 DLC

### 7.1 模块结构

`dungeon.js` 是 IIFE（立即执行函数），内部变量不污染全局。通过 `window.Dungeon` 暴露接口供主文件调用。

### 7.2 地牢状态 (`D`)

| 字段 | 类型 | 说明 |
|------|------|------|
| `active` | bool | 当前是否地牢模式 |
| `attrs` | {str,wis,brv,end,dex,luk} | 六大属性（初值0） |
| `gold` | int | 金币 |
| `stress` | int | 当前压力值 |
| `stressThreshold` | int | 压力触发阈值（40） |
| `curses` | {key:level} | 已获诅咒及等级 |
| `tempCurses` | array | Boss关临时诅咒 |
| `items` | array | 已获道具 |
| `shopVisitCount` | int | 商城访问次数（控制P/T轮替） |
| `timeBuyCount` | int | P交易购买次数（跨轮累计） |
| `goldBuyCount` | int | T交易购买次数（每轮重置） |
| `bossActive` | bool | 当前是否Boss关 |

### 7.3 六大属性及效果

| 属性 | 键 | 效果 | 公式 |
|------|-----|------|------|
| 力量 | str | 段位积分额外累积 | `score × (0.05 + Lv×0.03)` |
| 智慧 | wis | 降低时间流速 | `流速 × (1 - min(0.5, Lv×0.05))` |
| 勇气 | brv | 降低错牌惩罚 | `惩罚 × (1 - min(0.6, Lv×0.08))` |
| 耐力 | end | 降低压力累积 | `压力 × (1 - min(0.6, Lv×0.07))` |
| 技巧 | dex | 提升炫彩概率 | 炫彩判定+ `Lv×0.02` |
| 运气 | luk | 商品品质/降低高层诅咒概率 | `Lv×0.05` |

### 7.4 金币系统

| 来源 | 数量 |
|------|------|
| 升段（小学/初中/高中/无尽） | 3/5/7/8G |
| 普通连击爆发 | 1G |
| 完美连击爆发 | 4G |
| Boss段位额外 | 3G |

### 7.5 压力值系统

- **阈值**：40（达到即触发诅咒，溢出保留）
- **来源**：错牌(第3次起 +1~)、超时(+2/层)、PASS(+3)、升段(+3~+8)
- **触发**：满值时减阈值溢出保留 → 随机添加1层诅咒 → 紫色全屏特效
- **耐力减免**：压力获取量 × (1 - 耐力减免)

### 7.6 诅咒列表

| 诅咒 | 满级 | 效果 | 视觉 |
|------|------|------|------|
| 恍惚 | Lv.5 | N个字母透明度异步闪烁 | 字母淡入淡出 |
| 幻觉 | Lv.3 | N个字母随机互换位置 | 字母滑动交换 |
| 失衡 | Lv.5 | N个字母倒转显示 | 翻转180° |
| 乏力 | 无上限 | 段位积分累计降低(-15%起) | 积分条变暗 |
| 后悔 | 无上限 | 连击中断扣分(Lv.3起扣时间) | 屏幕震动 |
| 镣铐 | 无上限 | 答题流速+N×10% | 计时条颜色加深 |

### 7.7 Boss 关

- **触发**：`(stageIdx+1) % 3 === 0`，即小学3星(stageIdx=2)、初中3星(5)、高中3星(8)，无尽每3级
- **效果**：临时1级诅咒（通过后移除）、段位积分条变红、额外+3G

### 7.8 强化商城

开始（`stageIdx >= 3`），每次升段后
- **布局**：4个分区，瀑布流可滚动
  - G金币交易：6槽（2×3）+ 刷新
  - P分数交易/T时间交易：3槽 + 刷新（轮流出现，`shopVisitCount` 奇偶控制）
  - C诅咒交易：2槽（无刷新）
- **进入时**：`globalFlowRate=0`（时间暂停）
- **退出时**：0.3s 缓冲后恢复流速

### 7.9 主文件集成点

| 主文件 hook | 调用的 Dungeon 接口 |
|-------------|-------------------|
| `beginSoloGame()` | `Dungeon.init()` / `Dungeon.reset()` |
| `soloStartTimer()` | `getFlowRateMultiplier()` + `_shackleFlowBonus` |
| `soloStartWordTimer()` | 同上 |
| 错牌处理 | `Dungeon.addStress()` |
| `soloPassAction()` | `Dungeon.addStress(3)` |
| `soloCheckComplete()` | `getPromoScoreBonus()` + `getFatigueReduction()` + `applyRegret()` |
| 连击爆发 | `Dungeon.addGold()` |
| `soloPromote()` | `addGold()` + `addStress()` + `checkBoss()` + `shouldShowShop()` |
| `soloRollShiny()` | `getShinyBonus()` |
| `soloAddWordBook()` | `isActive()` → skip |
| `soloEndGame()` | `Dungeon.reset()` |

---

## 八、HTML DOM 关键节点

| ID / 选择器 | 说明 |
|-------------|------|
| `#mainMenu` | 主菜单 |
| `#soloPanel` | 单人模式面板 |
| `#soloPanel .solo-top` | 生命+暂停行 |
| `#soloPanel .solo-promo-area` | 段位名+积分条 |
| `#soloPanel .solo-word-section` | 题目区域（含星星/中文/字母槽/提示） |
| `#soloPanel .solo-cards-section` | 手牌+Pass按钮 |
| `#soloPanel .solo-book-section` | 单词簿/状态区 |
| `#soloPanel .solo-combo-box` | 连击显示 |
| `#dungeonShopArea` | 地牢商城容器（段位条下方） |
| `#dungeonGoldDisplay` | 金币显示（动态注入） |
| `#dungeonStressBar` | 压力条（动态注入） |
| `#soloTimerWrap` | 计时器 |
| `#soloTimer` | 时间数字 |
| `#soloWordTimerFill` | 答题进度条填充 |
| `#soloWordTimerGhost` | 答题进度条鬼影 |
| `#submodeStudy` / `#submodeDungeon` | 子模式按钮 |
| `#soloBook` | 单词簿内容区 |
| `#soloBookLabel` | 单词簿标签（地牢模式改为"状态"） |

---

## 九、数据流简图

### 单人模式答题循环

```
beginSoloGame → soloSetupWord → soloStartWordTimer
       ↑                              ↓
       │                        [出牌/倒计时]
       │                              ↓
       │                    soloCheckComplete (单词完成)
       │                    或 soloApplyWrongPenalty (罚时)
       │                    或 soloPassAction (跳过)
       │                              ↓
       │                        计分/升段判定
       │                              ↓
       │                    soloPromote / soloPromoFail
       │                              │
       │              ┌───────────────┘
       │              ↓ (地牢模式)
       │         dungeon 商城 → dungeonCloseShop
       │              └───────────────┘
       │                              ↓
       └──────────────────── soloSetupWord (下一题)
```

### 商城界面渲染

```
dungeonOpenShop
  → 隐藏 .solo-word-section / .solo-cards-section / .solo-book-section
  → 隐藏 .solo-promo-area (段位积分条)
  → globalFlowRate = 0 (时间暂停)
  → dungeonRenderShop
      → dungeonRenderGSection (6 slots)
      → dungeonection 或 dungeonRenderTSection (3 slots, 轮流)
      → dungeonRenderCSection (2 slots)

dungeonCloseShop
  → 恢复上述隐藏元素
  → setTimeout(300ms) 恢复 globalFlowRate
  → soloSetupWord (继续游戏)
```

---

## 十、配置文件

| 文件 | 说明 |
|------|------|
| `words.json` | 词库，按 easy/medium/hard 分类，每词 `{en, cn}` |
| `字母手牌.txt` | 可选手牌字母（空格分隔） |
| `DIFF_CONFIG` | PVP难度配置（题数/时间/空格范围） |
| `SOLO_STAGES` | 单人段位配置表 |
| `DUNGEON_CFG` | 地牢模式全量可配置参数 |

---

## 十一、开发注意事项

1. **不要直接修改 index.html 中的样式和内联脚本**，优先在 `dungeon.js` 中通过 CSS class 注入样式。
2. 地牢功能通过 `window.Dungeon` 全局接口调用，不要在 index.html 中硬编码地牢逻辑。
3. 存档键为 `grabword_save` (localStorage)，包含段位最高纪录、词库版本、分数记录等。
4. 所有时间驱动操作通过 `setInterval` 实现（100ms tick），`globalFlowRate` 统一控制所有计时速度。
5. Boss 关判定使用 `(stageIdx+1)%3===0`（小学3星 idx=2，依此类推），无尽每3级。
6. 压力值溢出保留机制：触发诅咒后 `stress -= threshold`，不丢弃溢出部分。
